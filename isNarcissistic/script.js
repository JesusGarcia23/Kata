function isNarcissistic(n){

    return theSum = n.toString().split('').map(Number).reduce((counter, eachNumber) => {
      return counter + Math.pow(eachNumber, n.toString().length)
    }, 0) === n
  
  }